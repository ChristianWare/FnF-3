import { Space_Mono } from "next/font/google";

export const spaceMono = Space_Mono({
  weight: ["400", "700"],
  style: ["normal"],
  subsets: ["latin"],
  variable: "--spaceMono",
  adjustFontFallback: false,
  display: "swap",
});
